const x = 234.212;
Math.round(x); // x 반올림
Math.floor(x); // x 내림
Math.ceil(x); // x 올림
Math.trunc(x); // x를 소수부분 제거하여 정수 변환

Math.abs(x); // x를 절대값 반화
Math.max(a, b, c); // a,b,c 제일 큰 숫자 돌려주기
Math.min(a, b, c); // a,b,c 제일 작은 숫자 돌려주기
Math.pow(x, y); // x의 y 제곱 -> x ** y
